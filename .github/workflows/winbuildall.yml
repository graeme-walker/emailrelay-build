name: winbuildall

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_job:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: build_step
        shell: cmd
        run: winbuildall.bat
