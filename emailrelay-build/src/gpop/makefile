# src/gpop/makefile -- generated by ..\libexec\make2nmake

ARCH = x64
CONFIG = release
MBEDTLS_SRC_ROOT = ../../../../mbedtls
MBEDTLS_BUILD_ROOT = ../../../../mbedtls
QT_SRC_ROOT = ../../../../qt-$(ARCH)-static
QT_BUILD_ROOT = ../../../../qt-$(ARCH)-static
QT_MOC = $(QT_BUILD_ROOT)/bin/moc.exe
SRC_ROOT = ../../..
BUILD_ROOT = ../..

all: subdirs libs programs

subdirs:

libs: gpop.lib

gpopextra.lib: 
    link /lib /nologo /out:gpopextra.lib 

gpop.lib: gpop_enabled.obj gpopserver.obj gpopstore.obj gpopserverprotocol.obj
    link /lib /nologo /out:gpop.lib gpop_enabled.obj gpopserver.obj gpopstore.obj gpopserverprotocol.obj

programs: 

CPPFLAGS = /nologo /DGCONFIG_NO_GCONFIG_DEFS /DG_QT_STATIC /EHsc /I. /D"G_SPOOLDIR=c:/emailrelay" /D"G_SYSCONFDIR=c:/emailrelay" /DG_WINDOWS=1 /DWIN32 /D_WINDOWS /DNDEBUG /W4 /WX /MT /O2 /Ob2 /I../. /I../.. /I../../../src/glib /I../../../src/gssl /I../../../src/gnet /I../../../src/gauth

gpop_enabled.obj: $(SRC_ROOT)/src/gpop/gpop_enabled.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopserver.obj: $(SRC_ROOT)/src/gpop/gpopserver.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopserverprotocol.obj: $(SRC_ROOT)/src/gpop/gpopserverprotocol.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopstore.obj: $(SRC_ROOT)/src/gpop/gpopstore.cpp
    $(CPP) $(CPPFLAGS) /c $?


