# src/makefile -- generated by ..\emailrelay\libexec\make2nmake

ARCH = x86
CONFIG = release
MBEDTLS_SRC_ROOT = ../../mbedtls
MBEDTLS_BUILD_ROOT = ../../mbedtls-$(ARCH)
QT_SRC_ROOT = ../../qt-$(ARCH)
QT_BUILD_ROOT = ../../qt-$(ARCH)
QT_MOC = $(QT_BUILD_ROOT)/bin/moc.exe
SRC_ROOT = ../..\emailrelay
BUILD_ROOT = ..

all: subdirs libs programs

subdirs:
    cd glib && nmake -f makefile
    cd gssl && nmake -f makefile
    cd gnet && nmake -f makefile
    cd gauth && nmake -f makefile
    cd gstore && nmake -f makefile
    cd gsmtp && nmake -f makefile
    cd gpop && nmake -f makefile
    cd win32 && nmake -f makefile
    cd gfilters && nmake -f makefile
    cd gverifiers && nmake -f makefile
    cd main && nmake -f makefile
    cd gui && nmake -f makefile

libs: 

programs: 


