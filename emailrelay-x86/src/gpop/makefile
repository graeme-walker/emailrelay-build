# src/gpop/makefile -- generated by ..\emailrelay\libexec\make2nmake

ARCH = x86
CONFIG = release
MBEDTLS_SRC_ROOT = ../../../mbedtls
MBEDTLS_BUILD_ROOT = ../../../mbedtls-$(ARCH)
QT_SRC_ROOT = ../../../qt-$(ARCH)
QT_BUILD_ROOT = ../../../qt-$(ARCH)
QT_MOC = $(QT_BUILD_ROOT)/bin/moc.exe
SRC_ROOT = ../../..\emailrelay
BUILD_ROOT = ../..

all: subdirs libs programs

subdirs:

libs: gpop.lib

gpopextra.lib: 
    link /lib /nologo /out:gpopextra.lib 

gpop.lib: gpop_enabled.obj gpopserver.obj gpopstore.obj gpopserverprotocol.obj
    link /lib /nologo /out:gpop.lib gpop_enabled.obj gpopserver.obj gpopstore.obj gpopserverprotocol.obj

programs: 

CPPFLAGS = /nologo /DGCONFIG_NO_GCONFIG_DEFS /DG_QT_STATIC /EHsc /I. /D"G_SPOOLDIR=c:/emailrelay" /D"G_SYSCONFDIR=c:/emailrelay" /DG_WINDOWS=1 /DWIN32 /D_WINDOWS /DNDEBUG /W4 /WX /MT /O2 /Ob2 /I. /I.. /I$(SRC_ROOT)/src/glib /I$(SRC_ROOT)/src/gssl /I$(SRC_ROOT)/src/gnet /I$(SRC_ROOT)/src/gauth

gpop_enabled.obj: $(SRC_ROOT)/src/gpop/gpop_enabled.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopserver.obj: $(SRC_ROOT)/src/gpop/gpopserver.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopserverprotocol.obj: $(SRC_ROOT)/src/gpop/gpopserverprotocol.cpp
    $(CPP) $(CPPFLAGS) /c $?

gpopstore.obj: $(SRC_ROOT)/src/gpop/gpopstore.cpp
    $(CPP) $(CPPFLAGS) /c $?


